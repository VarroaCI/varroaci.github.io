# =============================================================================
# ControllerClass — cluster-level defaults template for Controllers.
#
# A ControllerClass layers defaults between ProvisioningDefaults and each
# Controller's own spec, selected via spec.className. This example defines a
# "prod-large" class for production controllers with generous resources.
#
# ControllerClass is cluster-scoped (not namespaced).
#
# Usage:
#   kubectl apply -f examples/controller-class.yaml
#   kubectl get controllerclass
# =============================================================================
apiVersion: varroa.dev/v1alpha1
kind: ControllerClass
metadata:
  name: prod-large
spec:
  # Pod scheduling constraints
  nodeSelector:
    topology.kubernetes.io/zone: us-east-1a
  tolerations:
    - key: "dedicated"
      operator: "Equal"
      value: "jenkins"
      effect: "NoSchedule"
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - varroa-controller
            topologyKey: kubernetes.io/hostname

  # Container resources and storage
  resources:
    requests:
      cpu: "4"
      memory: 16Gi
    limits:
      cpu: "8"
      memory: 32Gi
  persistence:
    size: 100Gi
    storageClass: openebs-hostpath

  # A `mite:` block with an `image:` would go here. It is deliberately omitted:
  # the mite ships inside the main Varroa image, and with no override the
  # operator hands each mite its own image, so mites stay on the same version as
  # the operator and gateway they talk to. Set it only to pin a mite build
  # deliberately — it converges live on an already-Connected Controller, and a
  # floating tag here will skew mites away from the installed chart version.

  # Ingress defaults (converge live)
  ingressClassName: nginx
  ingressAnnotations:
    cert-manager.io/cluster-issuer: letsencrypt-route53

  # Pod metadata
  podLabels:
    environment: production
  podAnnotations:
    sidecar.istio.io/inject: "true"

  # Security
  securityContext:
    runAsUser: 1000
    runAsGroup: 1000
    fsGroup: 1000

  # Image pull secret names
  imagePullSecrets:
    - ghcr-credentials

  # JVM options (prepended to JAVA_OPTS)
  jvmOpts: "-XX:+UseG1GC -Xms4g"
