{
  "name": "varroa-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "node scripts/sync-docs.mjs && node scripts/validate-compare.mjs && node scripts/check-prose.mjs --strict",
    "dev": "astro dev",
    "prebuild": "node scripts/sync-docs.mjs && node scripts/validate-compare.mjs && node scripts/check-prose.mjs --strict",
    "build": "astro build",
    "postbuild": "node scripts/check-brochure-links.mjs && node scripts/check-section-order.mjs",
    "preview": "astro preview",
    "test": "node --test scripts/*.test.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.7",
    "astro": "^7.2.4",
    "mermaid": "^11.17.2",
    "starlight-links-validator": "^0.25.3"
  }
}
