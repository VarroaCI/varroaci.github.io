---
// Starlight Head override: default head plus the mermaid renderer, which is what
// turns ```mermaid fences into diagrams on docs pages.
import Default from "@astrojs/starlight/components/Head.astro";
import MermaidRenderer from "./MermaidRenderer.astro";
import DocsBackdrop from "./DocsBackdrop.astro";
import Analytics from "./Analytics.astro";
---

<Default><slot /></Default>
<MermaidRenderer />
<DocsBackdrop />
<Analytics />
